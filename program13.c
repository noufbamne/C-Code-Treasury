// Write a program to calculate the distance covered by a car travelling at the certainspeed for the given time

#include<stdio.h>
void main()
{
    // analyse the given program and rewrite it using proper variable names and method

    // int bingo, schmingo, zingo;

    // bingo = 2;
    // schmingo = 13;

    // zingo = bingo * schmingo;
    //  printf("%d , %d , %d\n", bingo,schmingo,zingo);

    // correct program

    int speed = 10,
        time = 20,
        distance;

        distance = 10 * 20;

        printf("The car travelling at %d MPH covers %d km distance in %d hours.", speed, distance, time);

}