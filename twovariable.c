// A simple program with two declaration
#include<stdio.h>
int main()
{
    // int minutes,hours;
    
    // minutes=60;
    // hours=24;

    // printf("There are %d minutes in an hour.\n",minutes);
    // printf("There are %d hours in a day.",hours);

    // getch();

    // Prints value of two numbers
    int units,dozens;
    dozens =17;
    units=dozens*12;

    printf("%d units is equivalent to %d dozen.\n",units ,dozens);
    getch();
}