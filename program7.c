// Program tp compute the average of three integers

#include<stdio.h>
void main()
{
    int a = 7,
    b = 12,
    c = 6,
    average = (a + b + c)/3;    //Combined declaration and initialization

    printf("The average of %d, %d and %d is %d", a, b ,c, average);
}